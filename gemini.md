# Gemini Memory

## Mandatory Rules
- **Compilation Check:** Every time code is modified (Rust or Frontend), I MUST run the appropriate build command (`cargo build` or `npm run build`) to verify there are no errors before confirming completion.